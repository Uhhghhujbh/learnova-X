export const fetchMetadata = async (url: string): Promise<{ title: string; description: string; image: string }> => { try { const response = await fetch(`https://api.microlink.io?url=${encodeURIComponent(url)}`); const data = await response.json(); return { title: data.data.title || 'No title', description: data.data.description || 'No description', image: data.data.image?.url || '' }; } catch { return { title: 'Failed to fetch', description: 'Could not retrieve metadata', image: '' }; } };